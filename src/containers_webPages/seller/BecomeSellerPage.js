import React, {useState} from "react";
import Typography from "@material-ui/core/Typography"

const BecomeSellerPage = (props)=>{
    return(
        <div>
            <h2>Im in become seller page</h2>
            <Typography variant="h2">
                Hello
            </Typography>
        </div>
    )
}

export default BecomeSellerPage;