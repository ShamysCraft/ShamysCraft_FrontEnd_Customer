import React from "react"
import {COLOURS} from "../../theme/colors"
const Footer = (props)=>{
    return(
        <div style={{backgroundColor : COLOURS.slightDarkGrey}} className="Footer">
        <h1>Im the Footer</h1>
    </div>
    )
}

export default Footer